/**
 * CZero Docs Configuration
 * Uses default tokens - no custom component overrides
 * This ensures Theme Customizer can modify all values via CSS variables
 */

export default {
  // Empty config = use all defaults with CSS variable references
  // This allows the Theme Customizer to work properly
};
